)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2021-Jun',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2021-Jun.csv')
# TWEET RETRIEVAL----
tweets <- get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2021-07-01T00:00:00Z',
end_tweets = '2021-07-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2021-Jul',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2021-Jul',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2021-Jul.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2021-08-01T00:00:00Z',
end_tweets = '2021-08-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2021-Aug',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2021-Aug',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2021-Aug.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2021-09-01T00:00:00Z',
end_tweets = '2021-09-30T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2021-Sep',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2021-Sep',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2021-Sep.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2021-10-01T00:00:00Z',
end_tweets = '2021-10-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2021-Oct',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2021-Oct',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2021-Oct.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2021-11-01T00:00:00Z',
end_tweets = '2021-11-30T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2021-Nov',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2021-Nov',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2021-Nov.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2021-12-01T00:00:00Z',
end_tweets = '2021-12-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2021-Dec',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2021-Dec',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2021-Dec.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-01-01T00:00:00Z',
end_tweets = '2022-01-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Jan',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2022-Jan',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2022-Jan.csv')
# BIND TWEETS----
# specify folder containing JSON files
setwd('~/UCF/Research/Neurodiversity/NeurDi/CSV/2022')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-02-01T00:00:00Z',
end_tweets = '2022-02-28T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Feb',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2022-Feb',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2022-Feb.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-03-01T00:00:00Z',
end_tweets = '2022-03-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Mar',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-03-01T00:00:00Z',
end_tweets = '2022-03-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Mar',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2022-Mar',
verbose = T,
output_format = 'tidy')
tail(bind$created_at)
# verify range in time
glimpse(bind)
# export csv
write.csv(bind, '2022-Mar.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-04-01T00:00:00Z',
end_tweets = '2022-04-30T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Apr',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2022-Apr',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2022-Apr.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-05-01T00:00:00Z',
end_tweets = '2022-05-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-May',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2022-May',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2022-May.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-06-01T00:00:00Z',
end_tweets = '2022-06-30T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Jun',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2022-Jun',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2022-Jun.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-07-01T00:00:00Z',
end_tweets = '2022-07-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Jul',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2022-Jul',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2022-Jul.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-08-01T00:00:00Z',
end_tweets = '2022-08-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Aug',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2022-Aug',
verbose = T,
output_format = 'tidy')
# export csv
write.csv(bind, '2022-Aug.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-09-01T00:00:00Z',
end_tweets = '2022-09-30T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Sep',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2022-Sep',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2022-Sep.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-010-01T00:00:00Z',
end_tweets = '2022-010-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Oct',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-010-01T00:00:00Z',
end_tweets = '2022-010-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Oct',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-10-01T00:00:00Z',
end_tweets = '2022-10-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Oct',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2022-Oct',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2022-Oct.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-11-01T00:00:00Z',
end_tweets = '2022-11-30T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Nov',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2022-Nov',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2022-Nov.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2022-12-01T00:00:00Z',
end_tweets = '2022-12-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2022-Dec',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2022-Dec',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2022-Dec.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2023-01-01T00:00:00Z',
end_tweets = '2023-01-31T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2023-Jan',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2023-Jan',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2023-Jan.csv')
# TWEET RETRIEVAL----
get_all_tweets(
query= 'neurodiversity', '#neurodiversity',
start_tweets = '2023-02-01T00:00:00Z',
end_tweets = '2023-02-28T00:00:00Z',
bearer_token = get_bearer(),
n = 50000,
data_path = '~/UCF/Research/Neurodiversity/NeurDi/2023-Feb',
export_query = T,
bind_tweets = F,
page_n = 500,
context_annotations = T,
verbose = T,
)
bind <- bind_tweets(
'~/UCF/Research/Neurodiversity/NeurDi/2023-Feb',
verbose = T,
output_format = 'tidy')
# verify range in time
glimpse(bind)
tail(bind$created_at)
# export csv
write.csv(bind, '2023-Feb.csv')
